package objects;

class SpineCharacter extends Character {
    public function new(json:Dynamic) {
        super();
        // load using SpineLoader
    }

    override function playAnim(name:String):Void {
        // spine animation switching
    }
}
